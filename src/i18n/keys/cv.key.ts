'use strict';
const namespace = 'cv';

export const cvKeys = Object.freeze({
  email: `${namespace}.email`,
  password: `${namespace}.password`,
  name: `${namespace}.name`,
  c_password: `${namespace}.c_password`,
  updateNow: `${namespace}.updateNow`,
  or: `${namespace}.or`,
});
